
# EXIT CODES

3 means that the file provided is not a .ber file
4 means that the file provided does not exist or does not have read permission
5 means that the file provided contains an invalid caracter
6 means that a malloc failed
7 means that there is not exactly 1 player start
8 means that the map is not delimited by ones
9 means that the map is not rectangular
